# Cargo Closed Launch Results — Phase 96

## 1. Цель

Разобрать результаты закрытого запуска карго, исправить реальные P0/P1 и решить, можно ли расширять тест.

## 2. Период закрытого запуска

_заполнить вручную_ (ожидалось 3–7 дней после Phase 95)

На момент Phase 96 в `docs/CARGO_CLOSED_LAUNCH_PHASE_95.md` **таблица проблем пуста**, метрики не заполнены оператором. Реальные отзывы участников ещё не внесены.

## 3. Количество участников

| Группа | План | Факт |
|---|---|---|
| Клиенты | 3–5 | _заполнить вручную_ |
| Карго-компании | 2–5 | _заполнить вручную_ |
| Admin / оператор | 1 | _заполнить вручную_ |

## 4. Метрики

| Метрика | Значение |
|---|---|
| Клиентов | _заполнить вручную_ |
| Карго-компаний | _заполнить вручную_ |
| Созданных карго-заявок | _заполнить вручную_ |
| Откликов | _заполнить вручную_ |
| Заявок с ≥1 откликом | _заполнить вручную_ |
| Подключённых Telegram | _заполнить вручную_ |
| Успешных Telegram-уведомлений | _заполнить вручную_ |
| P0 (из таблицы запуска) | 0 внесено |
| P1 (из таблицы запуска) | 0 внесено |
| Пользователей застряли | _заполнить вручную_ |
| Самые частые вопросы | _заполнить вручную_ |

Цифры не придумывались.

## 5. Проблемы P0

| ID | Источник | Описание | Статус |
|---|---|---|---|
| — | таблица Phase 95 | нет записей | — |
| — | code audit Phase 96 | подтверждённых P0 нет | — |

Privacy re-check (guest / non-owner / owner / responder / admin): утечек не найдено. Invalid UUID → not-found. Feedback CTA без env не ломает build (mailto fallback).

## 6. Проблемы P1

| ID | Источник | Описание | Статус |
|---|---|---|---|
| — | таблица Phase 95 | нет записей от пользователей | — |
| P96-01 | code audit | На `/account` в блоке «Мои заявки» статус карго показывался сырым enum (`NEW` / `IN_REVIEW` / …) | **Fixed** |

## 7. Проблемы P2/P3

| ID | Описание | Backlog |
|---|---|---|
| — | Нет внесённых пользовательских P2/P3 | — |
| note | `NEXT_PUBLIC_FEEDBACK_URL` желательно задать на production (WhatsApp/Telegram); без него работает mailto — **не P0** | ops |

## 8. Что исправлено

- P96-01: человекочитаемый статус + ссылка на `/cargo/requests/[id]` в `AccountRequestsSummary`
- Документы результатов / структура заполнения метрик и таблицы проблем

## 9. Что оставлено в backlog

- Заполнить метрики и таблицу проблем после реального прогона с 5–10 участниками
- Задать `NEXT_PUBLIC_FEEDBACK_URL` (опционально)
- Чат / рейтинги / платежи / тарифы / CRM — вне scope

## 10. Решение

**Нужен ещё один закрытый цикл** (сбор реальных результатов).

Причины:

1. Реальные проблемы и метрики из запуска **ещё не внесены** в Phase 95  
2. По коду: **нет P0**, один P1 закрыт — продукт не блокируется  
3. Расширять на большее число пользователей **нельзя утверждать**, пока оператор не заполнит метрики и таблицу после живого теста  

После заполнения §3–§4 и отсутствия открытых P0 → можно пересмотреть на «можно расширять тест».

### Feedback CTA

Проверено: `/cargo`, `/cargo/requests/[id]`, `/account/cargo-settings`, `/account/requests` — CTA на месте, mailto fallback без env.

### Сценарии (code / prior phases)

Клиент: create → success → detail → `/account/requests` — без новых P0.  
Карго: settings → Telegram → respond → confirmation — без новых P0 в audit.  
Privacy: guest/non-owner без телефона клиента и чужих контактов откликов.

---

## Phase 96 closed launch results and fixes

Results kit + P96-01 status label fix. Live metrics still operator-owned.
