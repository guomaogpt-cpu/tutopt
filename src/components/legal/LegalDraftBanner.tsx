import { LegalPageUpdateNote } from "@/components/legal/LegalPageUpdateNote";

/** @deprecated Use LegalPageUpdateNote — kept for import compatibility during migration. */
export function LegalDraftBanner() {
  return <LegalPageUpdateNote />;
}
